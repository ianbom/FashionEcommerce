services:
  app:
    build:
      args:
        APP_ENV: production
    environment:
      APP_ENV: production
      APP_DEBUG: "false"
      RUN_FRESH_SEED: "false"
    volumes:
      - storage:/var/www/html/storage

  web:
    ports:
      - "${APP_PORT:-80}:80"
    volumes:
      - storage:/var/www/html/storage:ro
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro

  queue:
    environment:
      APP_ENV: production
      APP_DEBUG: "false"
      RUN_FRESH_SEED: "false"
    volumes:
      - storage:/var/www/html/storage

  scheduler:
    environment:
      APP_ENV: production
      APP_DEBUG: "false"
      RUN_FRESH_SEED: "false"
    volumes:
      - storage:/var/www/html/storage

  vite:
    profiles:
      - never
